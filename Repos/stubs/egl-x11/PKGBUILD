# Maintainer: diced <pranaco2@gmail.com>
pkgbase=stubby
pkgname=egl-x11
pkgname+=\-"${pkgbase}"
pkgver=0.1.2
pkgrel=1
makedepends=()
provides=(  "${pkgname}"         )
conflicts=(  "${pkgname}"        )
arch=('x86_64')
pkgdesc="Stupid BullShit"

build() {
	echo "${pkgbase}"-"${pkgname}" >b
}

package() {
	echo CONGRATS
}

